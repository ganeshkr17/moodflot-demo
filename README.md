cd moodflot_railway_app  # zip se extract ki folder

git init
git remote add origin https://github.com/YourUsername/moodflot-demo.git
git add .
git commit -m "Initial Railway-ready demo app"
git push -u origin master
